# Python / Django
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Local secrets
.env

# Django
*.log
db.sqlite3

# Ad-hoc database backups (contain data — never commit)
backups/
*.dump
*.sql
# ...except the shared team snapshot, which IS tracked and passed via Git
!db/snapshot.sql
